// Generated by CoffeeScript 1.6.3
(function() {
  var arrowIsStarted, arrowStart, createNewArrow, createNewFunction, createNewPlaceholder;

  createNewFunction = function(name) {
    return ($("body")).append("<div class=\"ui-widget-content function\">\n    <div class=\"spacer\">\n        Function " + name + "\n    </div>\n</div>");
  };

  createNewPlaceholder = function(name) {
    return ($(".toolBox")).append("<div class=\"ui-widget-content placeholder\">\n    <div class=\"spacer\">\n        Function " + name + "\n    </div>\n</div>");
  };

  createNewArrow = function() {
    var arrowIsStarted;
    return arrowIsStarted = false;
  };

  createNewPlaceholder("foo");

  createNewPlaceholder("bar");

  arrowIsStarted = false;

  arrowStart = null;

  ($(".function")).click(function(event) {
    ($("body")).append("Function was clicked");
    if (arrowIsStarted) {
      return createNewArrow();
    } else {
      arrowIsStarted = true;
      return arrowStart = $(this);
    }
  });

  ($(".placeholder")).click(function(event) {
    createNewFunction("test");
    return ($(".function")).draggable();
  });

  ($(".function")).draggable();

}).call(this);
